#include "monty.h"
/**
 * f_nop- The opcode nop doesn’t do anything.
 * @head: stack head
 * @counter: line_number
 * Return: Successful
 */

void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
